{% extends app.request.get('_dashboard')=='DMS'
? 'NumaDOADMSBundle::layout.html.twig'
: 'NumaDOAAdminBundle::layout.html.twig' %}
 {% form_theme edit_form 'bootstrap_3_horizontal_layout.html.twig' %}
{% block content %}
    <h1>Image Carousel Edit</h1>

    {{ form(edit_form) }}

    {% if(app.request.get('_dashboard')=='DMS' and entity.componentId() is not empty) %}
        <a href="{{ path('component_edit', { 'id': entity.componentId() }) }}" class="btn btn-primary">
            Back to the component edit
        </a>
    {% elseif(app.request.get('_dashboard')=='DMS' and entity.dealerComponentId() is not empty) %}
        <a href="{{ path('dealercomponent_edit', { 'id': entity.dealerComponentId() }) }}" class="btn btn-primary">
            Back to the dealer component edit
        </a>
    {% else %}
        <a href="{{ path('dms_imagecarousel') }}" class="btn btn-primary">
            Back to the carousel
        </a>
    {% endif %}

{% endblock %}
