{% extends 'NumaDOADMSBundle::layout.html.twig' %}
 
{% block content %}
    <div class="page-header">
        <h1>Dashboard</h1>
    </div>
    <div class="box box-info">
        <div class="box-header with-border">
            <h3 class="box-title">Website Forms</h3>
            <div class="box-tools pull-right">
                <button class="btn btn-box-tool" data-widget="collapse" type="button">
                    <i class="fa fa-minus"></i>
                </button>
                <button class="btn btn-box-tool" data-widget="remove" type="button">
                    <i class="fa fa-times"></i>
                </button>
            </div>
        </div>
        <div class="box-body" style="display: block;">
            <div class="table-responsive">
                <table class="table no-margin">
                    <thead>
                    <tr>
                        <th>ID</th>
                        <th>Customer Name</th>
                        <th>Email</th>
                        <th>Date Submited</th>
                        <th>Type</th>
                        {#<th>Status</th>#}
                        <th>Action</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for entity in entities %}
                        <tr>
                            <td>
                                {% if entity.type|lower=="eprice" or entity.type|lower=="offer" or entity.type|lower=="contact" or entity.type|lower=="testdrive"  or entity.type|lower=="finance" %}
                                    <a href="{{ path('listingform_show', { 'id': entity.id }) }}">{{ entity.id }}</a>
                                {% elseif entity.type|lower=="financing" %}
                                    <a href="{{ path('finance_show', { 'id': entity.id }) }}">{{ entity.id }}</a>
                                {% elseif entity.type|lower=="service" %}
                                    <a href="{{ path('servicerequest_show', { 'id': entity.id }) }}">{{ entity.id }}</a>
                                {% elseif entity.type|lower=="part" %}
                                    <a href="{{ path('partrequest_show', { 'id': entity.id }) }}">{{ entity.id }}</a>
                                {% elseif entity.type|lower=="newsletter" %}
                                    <a href="{{ path('listingform_show', { 'id': entity.id }) }}">{{ entity.id }}</a>
                                {% endif %}
                            </td>
                            <td>{{ entity.cust_name }} {{ entity.cust_last_name }}</td>
                            <td>{{ entity.email }}</td>
                            <td>{{ entity.date_created|date('Y-m-d') }}</td>
                            <td>
                                {% if entity.type|lower=="eprice" %}
                                    <a href="{{ path('listingform') }}"><span class="label label-success">{{ entity.type }}</span></a>
                                {% elseif entity.type|lower=="contact" %}
                                    <a href="{{ path('listingform') }}"><span class="label label-danger">{{ entity.type }}</span></a>
                                {% elseif entity.type|lower=="part" %}
                                    <a href="{{ path('partrequest') }}"><span class="label label-info">{{ entity.type }}</span></a>
                                {% elseif entity.type|lower=="service" %}
                                    <a href="{{ path('servicerequest') }}"><span class="label label-warning">{{ entity.type }}</span></a>
                                {% elseif entity.type|lower=="offer" %}
                                    <a href="{{ path('listingform') }}"><span class="label label-default">{{ entity.type }}</span></a>
                                {% elseif entity.type|lower=="financing" %}
                                    <a href="{{ path('finance') }}"><span class="label label-default">{{ entity.type }}</span></a>
                                {% elseif entity.type|lower=="newsletter" %}
                                    <a href="{{ path('listingform') }}"><span class="label label-primary">{{ entity.type }}</span></a>
                                {% else %}
                                    <a href="{{ path('listingform') }}"><span class="label label-primary">{{ entity.type }}</span></a>
                                {% endif %}
                            </td>

                            <td>

                                    {% if entity.type|lower=="eprice" or entity.type|lower=="offer" or entity.type|lower=="contact" or entity.type|lower=="testdrive"  or entity.type|lower=="finance" %}
                                        <a href="{{ path('listingform_show', { 'id': entity.id }) }}" class="btn btn-primary">Show</a>
                                    {% elseif entity.type|lower=="financing" %}
                                        <a href="{{ path('finance_show', { 'id': entity.id }) }}" class="btn btn-primary">Show</a>
                                    {% elseif entity.type|lower=="service" %}
                                        <a href="{{ path('servicerequest_show', { 'id': entity.id }) }}" class="btn btn-primary">Show</a>
                                    {% elseif entity.type|lower=="part" %}
                                        <a href="{{ path('partrequest_show', { 'id': entity.id }) }}" class="btn btn-primary">Show</a>
                                    {% elseif entity.type|lower=="newsletter" %}
                                        <a href="{{ path('listingform_show', { 'id': entity.id }) }}" class="btn btn-primary">Show</a>
                                    {% endif %}

                            </td>

                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
        <div class="box-footer clearfix" style="display: block;">
            {#<a class="btn btn-sm btn-info btn-flat pull-left" href="javascript:void(0)">Place New Order</a>#}
            {#<a class="btn btn-sm btn-default btn-flat pull-right" href="javascript:void(0)">View All Orders</a>#}
        </div>
    </div>

    <div class="row">
        <div class="col-sm-2 col-xs-3">
            <div class="box box-danger box-solid">
                <div class="box-header with-border">
                    <h3 class="box-title">ALL LISTINGS</h3>
                </div>
                <div class="box-body" style="display: block;">
                    <div class="description-block border-right">
                        <h5 class="description-header">{{ stats.totalListings }}</h5>
                        <span class="description-text">TOTAL LISTINGS</span>
                    </div>
                    <div class="description-block border-right">
                        <h5 class="description-header">{{ stats.totalViews }}</h5>
                        <span class="description-text">TOTAL VIEWS</span>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-sm-2 col-xs-3">
            <div class="box box-danger box-solid">
                <div class="box-header with-border">
                    <h3 class="box-title">CAR LISTINGS</h3>
                </div>
                <div class="box-body" style="display: block;">
                    <div class="description-block border-right">
                        <h5 class="description-header">{{ stats.totalCarListings }}</h5>
                        <span class="description-text">CAR LISTINGS</span>
                    </div>
                    <div class="description-block border-right">
                        <h5 class="description-header">{{ stats.totalCarViews }}</h5>
                        <span class="description-text">CAR VIEWS</span>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-sm-2 col-xs-3">
            <div class="box box-danger box-solid">
                <div class="box-header with-border">
                    <h3 class="box-title">MOTO LISTINGS</h3>
                </div>
                <div class="box-body" style="display: block;">
                    <div class="description-block border-right">
                        <h5 class="description-header">{{ stats.totalMotoListings }}</h5>
                        <span class="description-text">MOTO LISTINGS</span>
                    </div>
                    <div class="description-block border-right">
                        <h5 class="description-header">{{ stats.totalMotoViews }}</h5>
                        <span class="description-text">MOTO VIEWS</span>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-sm-2 col-xs-3">
            <div class="box box-danger box-solid">
                <div class="box-header with-border">
                    <h3 class="box-title">MARINE</h3>
                </div>
                <div class="box-body" style="display: block;">
                    <div class="description-block border-right">
                        <h5 class="description-header">{{ stats.totalMarineListings }}</h5>
                        <span class="description-text">MARINE LISTINGS</span>
                    </div>
                    <div class="description-block border-right">
                        <h5 class="description-header">{{ stats.totalMarineViews }}</h5>
                        <span class="description-text">MARINE VIEWS</span>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-sm-2 col-xs-3">
            <div class="box box-danger box-solid">
                <div class="box-header with-border">
                    <h3 class="box-title">AG LISTINGS</h3>
                </div>
                <div class="box-body" style="display: block;">
                    <div class="description-block border-right">
                        <h5 class="description-header">{{ stats.totalAgsListings }}</h5>
                        <span class="description-text">AG LISTINGS</span>
                    </div>
                    <div class="description-block border-right">
                        <h5 class="description-header">{{ stats.totalAgsViews }}</h5>
                        <span class="description-text">AG VIEWS</span>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-sm-2 col-xs-3">
            <div class="box box-danger box-solid">
                <div class="box-header with-border">
                    <h3 class="box-title">RVs LISTINGS</h3>
                </div>
                <div class="box-body" style="display: block;">
                    <div class="description-block border-right">
                        <h5 class="description-header">{{ stats.totalRvsListings }}</h5>
                        <span class="description-text">RVs LISTINGS</span>
                    </div>
                    <div class="description-block border-right">
                        <h5 class="description-header">{{ stats.totalRvsViews }}</h5>
                        <span class="description-text">Rvs VIEWS</span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://code.highcharts.com/highcharts.js"></script>
    <script src="https://code.highcharts.com/modules/exporting.js"></script>

    <div id="container" style="min-width: 310px; height: 400px; margin: 0 auto"></div>
    <div id="container1" style="min-width: 310px; height: 400px; margin: 20px auto"></div>

    <script>
        $(document).ready(function () {

            var options = {
                chart: {
                    renderTo: 'container',
                    type: 'spline'
                },
                series: [{}],
                title: {
                    text: 'Visitors On Site',
                    x: -20 //center
                },
                xAxis: {
                    categories: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun',
                        'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec']
                },
                yAxis: {
                    title: {
                        text: 'Visitors'
                    },
                    plotLines: [{
                        value: 0,
                        width: 1,
                        color: '#808080'
                    }]
                },
                legend: {
                    layout: 'vertical',
                    align: 'right',
                    verticalAlign: 'middle',
                    borderWidth: 0
                },

            };

            $.getJSON('{{ path("stats_api_month") }}', function (data) {
                var d = new Date();
                var n = d.getFullYear();
                options.series[0].name = n;
                options.series[0].data = data;
                var chart = new Highcharts.Chart(options);
            });

        });

        $(document).ready(function () {

            var options = {
                chart: {
                    renderTo: 'container1',
                    type: 'column'
                },
                series: [{}],
                title: {
                    text: 'Visitors On Site Last 30 Days',
                    x: -20 //center
                },
                yAxis: {
                    title: {
                        text: 'Visitors'
                    },
                    plotLines: [{
                        value: 0,
                        width: 1,
                        color: '#808080'
                    }]
                },
                legend: {
                    layout: 'vertical',
                    align: 'right',
                    verticalAlign: 'middle',
                    borderWidth: 0
                },

            };

            $.getJSON('{{ path("stats_api_day") }}', function (data) {
                var d = new Date();
                var n = d.getFullYear();
                options.series[0].name = n;
                options.series[0].data = data;
                var chart = new Highcharts.Chart(options);
            });

        });

    </script>
{% endblock %}