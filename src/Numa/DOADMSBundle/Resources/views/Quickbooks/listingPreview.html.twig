{% extends 'NumaDOADMSBundle::layout.html.twig' %}

{% block content %}
    <div class="box">
        <div class="box-header">
            <h3 class="box-title">QuickBooks Listing preview</h3>
        </div>
        <div class="box-body table-responsive no-padding">
            {% for po in QBPOs %}
                {{ dump(po) }}
            {% endfor %}
        </div>
    </div>
{% endblock %}

