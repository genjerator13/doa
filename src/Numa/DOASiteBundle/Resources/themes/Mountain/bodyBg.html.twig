{% block bodyBackground %}
    <style type="text/css">
        {% set imgBg = displayComponent('site_background','Image','dealer')   %}

        {% if imgBg is not empty %}
        body{
            background-image: url("{{ imgBg }}");
        }
        {% endif %}
    </style>
{% endblock %}