{% extends 'NumaDOASiteBundle::home-layout.html.twig' %}
{% block content %}

<h2>Business Catalog</h2>
<div class="list-group id="myTab">
    {% for key,cat in categories %}
        <a href="{{path('homepage_category', {'idcategory': cat.id,'category_name':cat.name})}}" class="list-group-item">{{cat.name}}</a>
                {%endfor%}
        </div>

{% endblock %}

